#include <iostream>
#include <string>

using namespace std;

/*int main() {
	int a;
	int arr[1000];
	string str[1000];
	cin >> a;

	for (int i = 0; i < a; i++) {
		cin >> arr[i] >> str[i];
		if (arr[i] > 8 || arr[i] < 1)
			return 0;
		if (str[i].length() > 20 || str[i].length() < 1)
			return 0;
	}

	for (int i = 0; i < a; i++) {
		for (int j = 0; j < str[i].length(); j++) {
			for (int k = 0; k < arr[i]; k++) {
				cout << str[i][j];
			}
		}
		cout << endl;
	}
	return 0;
}*/
